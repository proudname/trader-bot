export enum DecideEnum {
    BUY,
    SELL,
    DO_NOTHING
}

export enum TradingRuleStatus {
    ACTIVE,
    EXECUTED
}

export enum TradeAction {
    BUY,
    SELL
}

export enum BrokerType {
    Tinkoff,
    Binance
}